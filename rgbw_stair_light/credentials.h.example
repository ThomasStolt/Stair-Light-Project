// Copy to "credentials.h" and set WIFI_PASS.
// credentials.h is in .gitignore and will not be committed to the repo.

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

#define WIFI_SSID "linksys-n"
#define WIFI_PASS "YOUR_PASSWORD_HERE"

// OTA / mDNS: hostname (e.g. stairlight-testbed, stairlight)
#define OTA_HOSTNAME "stairlight-testbed"

#endif

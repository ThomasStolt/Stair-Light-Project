// Als "credentials.h" kopieren und WIFI_PASS eintragen.
// credentials.h steht in .gitignore und wird nicht ins Repo übernommen.

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

#define WIFI_SSID "linksys-n"
#define WIFI_PASS "DEIN_PASSWORT_EINTRAGEN"

// OTA / mDNS: Hostname (z.B. stairlight-testbed, stairlight-treppe)
#define OTA_HOSTNAME "stairlight-testbed"

#endif
